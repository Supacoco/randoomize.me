const initialState = {
    viewBox: '0 0 1 1',
    dotSize: 0.001,
    dotColor: '#900C3F',
    width: 500,
    height: 500,
    backgroundColor: '#1e1e1e'
}

const context = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export {
    context
}