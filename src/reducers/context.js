const initialState = {
    viewBox: '0 0 1 1',
    dotSize: 0.001,
    dotColor: 'chartreuse',
    width: 900,
    height: 900,
    backgroundColor: '#1e1e1e'
}

const context = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export {
    context
}